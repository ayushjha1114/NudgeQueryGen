import express from 'express';
// import { createMongoTransaction } from '../controllers/mongoController.js';

const router = express.Router();

// router.post('/mongo/transaction', createMongoTransaction);

export default router;
